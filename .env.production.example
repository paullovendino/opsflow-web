# Production on Vercel: use the SPA origin so Sanctum cookies stay first-party.
# vercel.json proxies /api, /sanctum, and /storage to Render.
# Do not point this at onrender.com (cross-site CSRF will fail).
VITE_API_BASE_URL=https://myopsflow.vercel.app
VITE_APP_NAME=OpsFlow
