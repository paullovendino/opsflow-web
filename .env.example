# Local: point at the Laravel API directly (no /api suffix).
# Axios calls /api/v1/... and /sanctum/csrf-cookie on this origin.
VITE_API_BASE_URL=http://localhost:8000

# Production on Vercel: see .env.production.example (same-origin proxy via vercel.json).

VITE_APP_NAME=OpsFlow
