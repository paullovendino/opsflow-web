# Local: point at the Laravel API directly.
VITE_API_BASE_URL=http://localhost:8000

# Production on Vercel (same-origin proxy via vercel.json): leave empty so
# requests go to /api and /sanctum on https://myopsflow.vercel.app
# VITE_API_BASE_URL=

VITE_APP_NAME=OpsFlow
